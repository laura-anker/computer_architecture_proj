LOC 6 ; BEGIN AT LOCATION 6

; === Set initial registers ===
AIR 0,5      ; R0 = 5
AIR 1,12     ; R1 = 12
AIR 2,6      ; R2 = 6 (value to shift/rotate)

; === NOT instructions ===
NOT 0,0      ; R0 = ~5 → 65530
NOT 1,1      ; R1 = ~12 → 65523

; === SRC instructions (count encoded directly, not from R3) ===
SRC 2,3,1,1  ; R2 << 3 logical → 48
SRC 2,3,0,1  ; R2 >> 3 logical → 6

; === RRC instructions (count encoded directly) ===
RRC 2,3,0,1  ; rotate right 3 logical → 3
RRC 2,3,1,1  ; rotate left 3 logical → 6

LOC 1024
End: HLT